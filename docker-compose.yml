services:
  internship-tracker:
    image: ronp2805/internship-tracker:latest
    restart: always
    volumes:
      - ./data:/data   # users.yaml, tracker.db, (later) cvs/ and output/
    env_file:
      - .env
